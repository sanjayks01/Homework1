#!/bin/bash
# this file must start the calculation external program on not Windows platforms (Linux, Mac OSX)
# the first special line set that this script will be evaluated with the bash Linux/Unix interpreter
# then here ony bash commands must be used (e.g. to rename files, delete old results, etc.)
# is is possible to use other shells instead of bash, like csh
#
# to be completed by the student...
